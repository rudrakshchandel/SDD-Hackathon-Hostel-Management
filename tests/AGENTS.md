# Tests Module Notes

## Scope
- `tests/` hosts unit-level regression tests.

## Guardrails
- Keep tests deterministic and fast.
- Prefer pure function tests over network-bound tests.
- Name test files `*.test.ts`.

## Validation
- Run `npm test` locally before commit.

